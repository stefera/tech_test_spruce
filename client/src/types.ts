export type XorO = 'X' | 'O';
export type Board = (XorO | undefined)[][];

export interface PlayerStats {
  name: string;
  wins: number;
  losses: number;
  draws: number;
}

export interface PlayerStats {
  name: string;
  wins: number;
  losses: number;
  draws: number;
}
